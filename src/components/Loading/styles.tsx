import { GlobalImage, GlobalText } from "@/styles/global";
import styled from "styled-components/native";

export const LoadingText = styled(GlobalText)`

`;

export const LoadingGif = styled(GlobalImage) `
    width:63px;
    height:63px;
`;